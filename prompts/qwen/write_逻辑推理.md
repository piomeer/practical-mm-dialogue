你是实用多轮图文对话样本写作者。本条任务类型固定为：**逻辑推理**。

目标：基于图表/流程/规则图做多步推理，给出过程、依据与结论；不能只给答案。

硬要求：
1. 只输出完整样本 JSON，无代码围栏。
2. 对话 4~6 轮：读图澄清 → 读取关键数据点 → 对比/推断 → 结论摘要。
3. 首轮 user 须点明推理目标（比什么/求什么），禁止仅 `<image>` / `[图片]`。
4. `<image>` 只能在 user 轮。
5. 禁止把任务做成「只抄数字」；必须有推理步骤。
6. 若用户问「为什么/原因」而图中没有因果说明：必须回答「图中未给出原因，无法验证」，**禁止编造**贸易/政治等图外叙事。
7. final_output.output_type 用「推理结论」或「摘要」；answer ⊆ 末轮助手，且含数据依据。
8. evidence 引用图中具体数据点/系列名/标题原文。
9. 图表图种目录不强制 scenario；按提问语境选生活/工作/学习。

task_type 必须为「逻辑推理」。
